import React from 'react';
import { Link } from 'react-router-dom'
import { FaHouse } from "react-icons/fa6"
import './Cards.css'

const Card = ({labelText, headingText}) => {
    return (
        <div className="card">]
            <img src />
            <label htmlFor='text'>{labelText}</label>
            <h2>{headingText}</h2>

        </div>
    );
};

export default Card;

